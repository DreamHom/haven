meta {
  name: Dayo
  seq: 6
}

docs {
  Dayo — The Platform Guardian. T&S admin running the morning queue.

  Walks: login → /me → verification queue (no type filter, status=APPROVED) →
  approve / reject → suspend → reactivate → listing takedown → listing approve
  (re-publish) → hunt for audit-logs + listing-reports admin queues (still
  missing — confirmed via spec absence).
}
