meta {
  name: 02 Emeka accepts Lekki assignment
  type: http
  seq: 2
}

post {
  url: {{host}}/api/agent-listings/{{assignment_lekki_id}}/accept
  body: none
  auth: bearer
}

auth:bearer {
  token: {{emeka_token}}
}

assert {
  res.status: eq 200
}
