meta {
  name: 06 Emeka accepts Bourdillon assignment
  type: http
  seq: 6
}

post {
  url: {{host}}/api/agent-listings/{{assignment_bourdillon_id}}/accept
  body: none
  auth: bearer
}

auth:bearer {
  token: {{emeka_token}}
}

assert {
  res.status: eq 200
}
