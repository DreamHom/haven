meta {
  name: 04 Emeka accepts Ikate assignment
  type: http
  seq: 4
}

post {
  url: {{host}}/api/agent-listings/{{assignment_ikate_id}}/accept
  body: none
  auth: bearer
}

auth:bearer {
  token: {{emeka_token}}
}

assert {
  res.status: eq 200
}
