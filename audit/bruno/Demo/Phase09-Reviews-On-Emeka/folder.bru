meta {
  name: Phase09-Reviews-On-Emeka
  seq: 9
}

docs {
  Reviews require a CLOSED listing where the reviewer was a deal participant
  (Phase08 set both up). Each applicant reviews Emeka — the assigned agent on
  the closed listing — so his profile shows credible ratings.
}
