meta {
  name: 01 Admin approves Amaka's OWNER_IDENTITY
  type: http
  seq: 1
}

post {
  url: {{host}}/api/admin/verifications/{{verification_amaka_id}}/approve
  body: none
  auth: bearer
}

auth:bearer {
  token: {{admin_token}}
}

assert {
  res.status: eq 200
}
